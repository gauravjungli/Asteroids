#include "gauravlib.h"
void shed(vector<CV>& w)
{
for ( int j = 2; j <= res; j++)
{

	if (Psi() > 0) // -1e-6
	{
		//cout << "mass shed";
		w[j].modify(  pow(10, -8),
			w[j].p * w[j].u,
		w[j].r = w[j].p * w[j].v);	
	}
}
}
double Psi()
{
return	0;//change for the sphere
}