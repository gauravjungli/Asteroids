#include "gauravlib.h"
void shed(vector<CV>& w,vector<double>& x)
{
for ( int j = 2; j <= res; j++)
			{

				if (phi > 0) // -1e-6
				{
					//cout << "mass shed";
					w[j].modify(  pow(10, -8),
					  	w[j].p * w[j].u,
					w[j].r = w[j].p * w[j].v);	
				}
			}
}
double phi(CV& w,double& x)
{
return	0;//change for the sphere
}